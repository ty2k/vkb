# v-k-b/react

This directory contains the `v-k-b` React component `Keyboard` and associated type information.

Library code is check in the `src/lib` folder.

An example application showing the usage of the component is in the `src/example` folder.

See root [README.md](/README.md) for component usage.
